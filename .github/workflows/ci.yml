name: AppDrop CI

on:
  push:
    branches: [main]
    tags: ['v*']
  pull_request:
    branches: [main]

jobs:
  test-engine:
    name: ✅ Python Engine Tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v4
        with: { python-version: "3.11" }
      - run: pip install fastapi uvicorn pydantic pytest pytest-asyncio httpx websockets
      - run: pytest tests/ -v --tb=short

  test-frontend:
    name: ✅ Frontend Tests + Build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: 20, cache: npm }
      - run: npm install
      - run: npm test
      - run: npm run build

  build-and-release:
    name: 📦 Build & Release
    needs: [test-engine, test-frontend]
    if: startsWith(github.ref, 'refs/tags/v')
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: macos-latest
            args: --target universal-apple-darwin
            rust-targets: aarch64-apple-darwin,x86_64-apple-darwin
          - os: windows-latest
            args: ''
            rust-targets: ''
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: 20, cache: npm }
      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.rust-targets }}
      - run: npm install

      # macOS only: import Apple certificate — skipped when secrets are absent
      - name: Import Apple certificate
        if: runner.os == 'macOS' && secrets.APPLE_CERTIFICATE != ''
        env:
          APPLE_CERTIFICATE: ${{ secrets.APPLE_CERTIFICATE }}
          APPLE_CERTIFICATE_PASSWORD: ${{ secrets.APPLE_CERTIFICATE_PASSWORD }}
        run: |
          echo "$APPLE_CERTIFICATE" | base64 --decode > certificate.p12
          security create-keychain -p appdrop-ci build.keychain
          security default-keychain -s build.keychain
          security unlock-keychain -p appdrop-ci build.keychain
          security import certificate.p12 -k build.keychain -P "$APPLE_CERTIFICATE_PASSWORD" -T /usr/bin/codesign
          security set-key-partition-list -S apple-tool:,apple: -s -k appdrop-ci build.keychain

      # Windows only: generate self-signed cert, export as base64 PFX
      - name: Generate Windows self-signed certificate
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          $cert = New-SelfSignedCertificate -Type Custom -Subject "CN=AppDrop, O=AppDrop" `
            -KeyUsage DigitalSignature -FriendlyName "AppDrop" `
            -CertStoreLocation "Cert:\CurrentUser\My" `
            -TextExtension @("2.5.29.37={text}1.3.6.1.5.5.7.3.3","2.5.29.19={text}")
          $pwd = ConvertTo-SecureString -String "appdrop-ci" -Force -AsPlainText
          Export-PfxCertificate -Cert "Cert:\CurrentUser\My\$($cert.Thumbprint)" `
            -FilePath certificate.pfx -Password $pwd
          $b64 = [Convert]::ToBase64String([IO.File]::ReadAllBytes("certificate.pfx"))
          echo "WINDOWS_CERTIFICATE=$b64" >> $env:GITHUB_ENV
          echo "WINDOWS_CERTIFICATE_PASSWORD=appdrop-ci" >> $env:GITHUB_ENV

      - uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          # macOS — only active when secrets are present (imported above)
          APPLE_CERTIFICATE: ${{ secrets.APPLE_CERTIFICATE }}
          APPLE_CERTIFICATE_PASSWORD: ${{ secrets.APPLE_CERTIFICATE_PASSWORD }}
          APPLE_SIGNING_IDENTITY: ${{ secrets.APPLE_SIGNING_IDENTITY }}
          APPLE_ID: ${{ secrets.APPLE_ID }}
          APPLE_PASSWORD: ${{ secrets.APPLE_PASSWORD }}
          APPLE_TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}
          # Windows — set by step above
          WINDOWS_CERTIFICATE: ${{ env.WINDOWS_CERTIFICATE }}
          WINDOWS_CERTIFICATE_PASSWORD: ${{ env.WINDOWS_CERTIFICATE_PASSWORD }}
          # Skip updater signing and binary stripping when no signing certs
          TAURI_PRIVATE_KEY: ''
          NO_STRIP: 1
        with:
          tagName: ${{ github.ref_name }}
          releaseName: 'AppDrop ${{ github.ref_name }}'
          releaseBody: 'Download the DMG (macOS) or MSI (Windows) below.'
          releaseDraft: false
          prerelease: false
          args: ${{ matrix.args }}
